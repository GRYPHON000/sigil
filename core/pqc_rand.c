/* sigil — core/pqc_rand.c
 * Cryptographic random bytes from /dev/urandom
 * Used by all key generation in sigil
 */
#include <stdio.h>
#include <stdint.h>

int sigil_rand_bytes(uint8_t *buf, size_t len) {
    /* implementation coming Day 2 */
    return 0;
}